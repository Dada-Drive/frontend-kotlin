# R-5.4 Session A — Rider Home Extraction + S10-S12

## Date
2026-05-20

## Périmètre Session A

- ✅ V1 Audit fichiers candidats extraction (rapport complet)
- ✅ V2 Création `presentation/riderhome/` + déplacement 6 fichiers rider-only
- ✅ V3 S10 RiderHomeScreen (idle bottom sheet)
- ✅ V4 S11 SearchDestinationScreen (route input)
- ✅ V5 S12 RoutePreviewScreen (route + fare estimate)
- ✅ V6 Validation extraction + build complet
- ✅ V7 Doc + ACTION_PLAN partiel

## Différé Session B (~2-3h)

- S13 SchedulePickerScreen (wheel picker date/time)
- S14 WaitingDriverScreen (après ride request)
- Paparazzi S13-S14 (4 snapshots restants)
- ACTION_PLAN final + bilan total R-5.4

## Fichiers déplacés (map/ → riderhome/)

| Ancien nom | Nouveau nom | LOC |
|---|---|---|
| `MapRiderDestinationConfirmedBar.kt` | `RiderDestinationConfirmedBar.kt` | 1 087 |
| `MapRouteSheet.kt` | `RouteSheet.kt` | 1 038 |
| `MapRoutePickerComponents.kt` | `RoutePickerComponents.kt` | 290 |
| `MapRouteInputComponents.kt` | `RouteInputComponents.kt` | 275 |
| `ScheduleForLaterSheet.kt` | `ScheduleForLaterSheet.kt` | 466 |
| `MapRideCards.kt` | `RideCards.kt` | ~250 |

Imports croisés ajoutés : `IntermediateStopDraft`, `PassengerRouteOption`,
`formatAddressForDisplay`, `AddressSearchHit`, `IntermediateStopValidity`,
`STOP_PIN_YELLOW_ARGB` (tous dans `map/` — shared infra).

## Fichiers créés (nouveaux dans riderhome/)

- `RiderHomeScreen.kt` — S10 idle overlay (greeting + search pill + shortcuts)
- `SearchDestinationScreen.kt` — S11 route input (origin/destination + connector)
- `RoutePreviewScreen.kt` — S12/S14 route + fare estimate card + CTA

## Préservé intact dans map/

- `HereMapViewComposable.kt` — core map rendering (pins + route + alternatives)
- `MapViewModel.kt` — shared VM, deferred refactor R-5.3 Session C
- 5 controllers délégués (Location, Routing, POI, RideOps, DriverPreview)
- `MapChrome.kt` (MapHomeTopHeader, MapSideFloatingControls — shared)
- `PickupPinOverlay.kt`, `TeardropPickupPinBitmap.kt`, `SnakeAnimator.kt`
- All POI files, utilities, models

## Architecture riderhome/ (résultat)

```
presentation/riderhome/
├── RideCards.kt                   (moved)
├── RiderDestinationConfirmedBar.kt (moved)
├── RiderHomeScreen.kt             (new — S10)
├── RouteInputComponents.kt        (moved)
├── RoutePickerComponents.kt       (moved)
├── RoutePreviewScreen.kt          (new — S12/S14)
├── RouteSheet.kt                  (moved)
├── ScheduleForLaterSheet.kt       (moved)
├── SearchDestinationScreen.kt     (new — S11)
└── components/                    (directory)
```

## Snapshots Paparazzi (Session A)

| Screen | Light | Dark |
|---|---|---|
| S10 `RiderHomeBottomSheet` | ✅ | ✅ |
| S11 `SearchDestinationContent` | ✅ | ✅ |
| S12 `RoutePreviewBottomSheet` | ✅ | ✅ |

Total Session A : **6 PNG** (cible R-5.4 complète : 10 PNG, reste 4 en Session B)

## Statut

- Session A : ✅ Closed
- R-5.4 global : 🟡 Partial (3/5 écrans — S10/S11/S12 done, S13/S14 Session B)

## Commits Session A (5)

```
cb75d88 refactor(architecture): extract rider-specific composables to presentation/riderhome/
d7ece5c feat(riderhome): align RiderHomeScreen (S10 idle) with redesign v2
16361a9 feat(riderhome): align SearchDestinationScreen (S11 route input) with redesign v2
d4a2b12 feat(riderhome): align RoutePreviewScreen (S12/S14 route + fare) with redesign v2
444b4c0 test(riderhome): validate R-5.4 Session A extraction + full build
```
