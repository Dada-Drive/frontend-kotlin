# R-5.4 Session B — SchedulePickerScreen (S13) + WaitingDriverScreen (S14)

## Date
2026-05-20

## Périmètre Session B

- ✅ V1 Audit S13/S14 — réconciliation JSX vs brief (SchedulePicker + WaitingDriver)
- ✅ V2 S13 SchedulePickerScreen + SchedulePickerContent + 2 snapshots
- ✅ V3 S14 WaitingDriverScreen + WaitingForDriverContent + 2 snapshots
- ✅ V4 Validation R-5.4 complet (10 PNG + build release SUCCESSFUL)
- ✅ V5 Doc complet R-5.4 + ACTION_PLAN final

## Clarification JSX S13/S14

| Plan interne | JSX label | Contenu |
|---|---|---|
| S10 | JSX S10 | Rider home idle — ✅ Session A |
| S11 | JSX S11 | Route input — ✅ Session A |
| S12 | JSX S14 | Route + fare estimate — ✅ Session A (`RoutePreviewScreen`) |
| **S13** | **JSX S12** | **Schedule picker (wheel date/time)** |
| **S14** | **JSX S15** | **Waiting / Searching drivers** |

JSX S13 (Pickup pin) = map-interactif, hors scope Paparazzi.
`FareEstimateScreen` du brief = fare estimate déjà couvert par `RoutePreviewScreen` (Session A).

## Fichiers créés (Session B)

| Fichier | LOC | Description |
|---|---|---|
| `SchedulePickerScreen.kt` | ~270 | Wrapper + `SchedulePickerContent` + `ScheduleStaticWheel` |
| `WaitingDriverScreen.kt` | ~250 | Wrapper + `WaitingForDriverContent` |

Strings ajoutées (`strings.xml`) : `waiting_driver_title/subtitle/view_cta/cancel`

## Architecture finale riderhome/ (R-5.4 complet)

```
presentation/riderhome/
├── RideCards.kt                    (moved — Session A)
├── RiderDestinationConfirmedBar.kt (moved — Session A)
├── RiderHomeScreen.kt              (new — S10, Session A)
├── RouteInputComponents.kt         (moved — Session A)
├── RoutePickerComponents.kt        (moved — Session A)
├── RoutePreviewScreen.kt           (new — S12, Session A)
├── RouteSheet.kt                   (moved — Session A)
├── ScheduleForLaterSheet.kt        (moved — Session A, preserved intact)
├── SchedulePickerScreen.kt         (new — S13, Session B)
├── SearchDestinationScreen.kt      (new — S11, Session A)
├── WaitingDriverScreen.kt          (new — S14, Session B)
└── components/                     (directory)
```

## Snapshots Paparazzi (Session A + B)

| Screen | Component | Light | Dark |
|---|---|---|---|
| S10 | `RiderHomeBottomSheet` | ✅ | ✅ |
| S11 | `SearchDestinationContent` | ✅ | ✅ |
| S12 | `RoutePreviewBottomSheet` | ✅ | ✅ |
| S13 | `SchedulePickerContent` | ✅ | ✅ |
| S14 | `WaitingForDriverContent` | ✅ | ✅ |

**Total R-5.4 : 10 PNG ✅** (cible atteinte)

## Preserved intact (per R-5.3 decisions)

- `HereMapViewComposable.kt` — core map rendering (pins + route + alternatives)
- `MapViewModel.kt` — shared VM (deferred refactor R-5.3 Session C)
- `ScheduleForLaterSheet.kt` — existing wheel picker (moved Session A, untouched Session B)
- 5 controllers délégués (Location, Routing, POI, RideOps, DriverPreview)

## Build validation

- `compileDebugKotlin` : ✅ SUCCESS
- `ktlintCheck + detekt` : ✅ clean (0 violations)
- `verifyPaparazziDebug` : ✅ 10 PNG verified
- `assembleRelease` : ✅ SUCCESS — `app-arm64-v8a-release.apk` (55 MB, R8 stable)

## Commits Session B (2)

```
3dbbff0 feat(riderhome): align WaitingDriverScreen (S14) with redesign v2
37900e0 feat(riderhome): align SchedulePickerScreen (S13) with redesign v2
```

## Statut R-5.4

- Session A : ✅ Closed (6 commits)
- Session B : ✅ Closed (2 commits)
- **R-5.4 global : ✅ CLOSED** (5/5 écrans, 10 PNG, release build ✅)

## Commits R-5.4 totaux (Session A + B)

```
3dbbff0 feat(riderhome): align WaitingDriverScreen (S14) with redesign v2
37900e0 feat(riderhome): align SchedulePickerScreen (S13) with redesign v2
15e5f52 docs: R-5.4 Session A complete — riderhome/ extracted, S10-S12 refactored
444b4c0 test(riderhome): validate R-5.4 Session A extraction + full build
d4a2b12 feat(riderhome): align RoutePreviewScreen (S12/S14 route + fare) with redesign v2
16361a9 feat(riderhome): align SearchDestinationScreen (S11 route input) with redesign v2
d7ece5c feat(riderhome): align RiderHomeScreen (S10 idle) with redesign v2
cb75d88 refactor(architecture): extract rider-specific composables to presentation/riderhome/
```

**Total : 8 commits LOCAUX, 0 push**
